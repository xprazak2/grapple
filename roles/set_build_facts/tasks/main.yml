---
- name: Set facts for image build
  set_fact:
    image_name: "{{ (registry == '') | ternary(image, [registry, image] | join('/')) }}"
    tag: "{{ (version == 'latest') | ternary(version, [version, release] | join('-')) }}"
    build_dir: "{{ lookup('env', 'PWD') }}/tmp"
